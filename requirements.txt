# Các thư viện chính của bot
python-telegram-bot==20.6  # Phiên bản Telegram bot mà bạn đang dùng

# Thư viện cho Web server (Flask)
Flask==3.1.1               # Phiên bản Flask ổn định

# Thư viện cho Supabase
supabase==2.10.0           # Phiên bản Supabase client (đã cập nhật để khắc phục lỗi 'proxy')
httpx                        # Bỏ ràng buộc phiên bản để pip tự động chọn phiên bản tương thích với supabase

# Thư viện cho Google Sheets (nếu bạn dùng gspread)
gspread
gspread-asyncio

# Các thư viện khác có thể cần thiết cho bot của bạn
# Nếu bạn sử dụng biến môi trường từ file .env khi chạy local
python-dotenv

# Nếu module AI của bạn sử dụng OpenRouter hoặc các API khác
# openrouter-py # Ví dụ nếu bạn có thư viện riêng cho OpenRouter
# requests # Nếu có bất kỳ phần nào của code sử dụng thư viện requests trực tiếp
